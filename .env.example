# Скопировать в .env и заполнить. .env в репозиторий не попадает.

# Учётная запись для SMB-шары с образами.
# На этапе 1 станет одноразовой, создаваемой на время монтирования.
SMB_USER=ikvm
SMB_PASS=

# Каталог с ISO на хосте.
ISO_DIR=/opt/iso
